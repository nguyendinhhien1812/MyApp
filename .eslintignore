# Worker của Cloudflare: runtime khác hẳn React Native (có Response,
# ReadableStream, TextEncoder...). Cấu hình eslint của RN không áp được ở đây;
# server/ có cổng riêng là `node --check` trong CI.
server/

# Mini-app build và deploy độc lập, dùng cấu hình rspack riêng
miniapps/

# Script vận hành chạy bằng Node thuần
tools/

# Kết quả build
ios/
android/

# Khai báo type do Module Federation sinh ra. Không sửa tay — và sở dĩ chúng
# nằm im trong repo là vì cấu hình đang đặt dts:false (xem CLAUDE.md), nên
# không có gì tái sinh chúng nữa.
@mf-types/
