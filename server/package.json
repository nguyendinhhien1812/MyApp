{
  "name": "myapp-ai-proxy",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Cloudflare Worker đứng giữa app MyApp và Gemini, giữ API key ở phía server",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "tail": "wrangler tail"
  },
  "devDependencies": {
    "wrangler": "^3.90.0"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.122.0"
  }
}
